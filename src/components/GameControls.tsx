import { useDispatch, useSelector } from 'react-redux'\nimport { RootState, AppDispatch } from '../store/store'\nimport { setGameSpeed, togglePause } from '../store/gameSlice'\nimport { Play, Pause, FastForward } from 'lucide-react'\n\nfunction GameControls() {\n  const dispatch = useDispatch<AppDispatch>()\n  const game = useSelector((state: RootState) => state.game)\n\n  return (\n    <div className=\"panel p-4\">\n      <h3 className=\"text-lg font-bold text-white mb-4\">Game Controls</h3>\n\n      <div className=\"space-y-3\">\n        {/* Pause Button */}\n        <button\n          onClick={() => dispatch(togglePause())}\n          className={`w-full py-2 rounded font-semibold transition-colors flex items-center justify-center gap-2 ${\n            game.isPaused\n              ? 'bg-green-600 hover:bg-green-700 text-white'\n              : 'bg-yellow-600 hover:bg-yellow-700 text-white'\n          }`}\n        >\n          {game.isPaused ? (\n            <>\n              <Play className=\"w-4 h-4\" />\n              Resume\n            </>\n          ) : (\n            <>\n              <Pause className=\"w-4 h-4\" />\n              Pause\n            </>\n          )}\n        </button>\n\n        {/* Speed Controls */}\n        <div className=\"space-y-2\">\n          <p className=\"text-sm text-industrial-400 font-semibold\">Game Speed</p>\n          <div className=\"grid grid-cols-3 gap-2\">\n            {[1, 2, 4].map((speed) => (\n              <button\n                key={speed}\n                onClick={() => dispatch(setGameSpeed(speed))}\n                className={`py-2 rounded font-semibold transition-colors text-sm ${\n                  game.gameSpeed === speed\n                    ? 'bg-production-weld text-white'\n                    : 'bg-industrial-700 hover:bg-industrial-600 text-industrial-200'\n                }`}\n              >\n                {speed}x\n              </button>\n            ))}\n          </div>\n        </div>\n\n        {/* Current Status */}\n        <div className=\"bg-industrial-700 p-3 rounded text-xs space-y-1\">\n          <div className=\"flex justify-between\">\n            <span className=\"text-industrial-400\">Status:</span>\n            <span className=\"font-semibold text-production-weld\">\n              {game.isPaused ? '⏸ Paused' : '▶ Running'}\n            </span>\n          </div>\n          <div className=\"flex justify-between\">\n            <span className=\"text-industrial-400\">Speed:</span>\n            <span className=\"font-semibold text-production-weld\">{game.gameSpeed}x</span>\n          </div>\n        </div>\n      </div>\n    </div>\n  )\n}\n\nexport default GameControls\n